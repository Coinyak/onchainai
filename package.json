{
  "name": "onchainai",
  "private": true,
  "description": "OnchainAI monorepo — Rust API (root) + Next.js frontend (frontend/)",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "vercel-build": "npm run build --workspace=frontend"
  }
}