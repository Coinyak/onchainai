/** ChainList — chain filtering moved to ChainStrip per UI spec. Placeholder for sidebar section. */
export function ChainList() {
  return (
    <p className="text-body-sm text-secondary px-2">
      Use the chain strip above the tool list to filter by chain.
    </p>
  );
}