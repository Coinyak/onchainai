//! CryptoSkill source crawler — stub.
//!
//! Implementation (reqwest + scraper) added in crawler-sources milestone.

pub async fn run_once(_pool: &sqlx::PgPool) {
    tracing::info!("cryptoskill crawl stub — not yet implemented");
}
