{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.api",
    "watchPatterns": [
      "src/**",
      "migrations/**",
      "Dockerfile.api",
      "Cargo.toml",
      "Cargo.lock",
      "railway.json"
    ]
  }
}