# Qodo (PR Agent) — manual review only. Do not auto-run on open/push PRs.
# Request explicitly in a PR comment when needed (e.g. /review).
# CodeRabbit: see .coderabbit.yaml (comment @coderabbitai review only).

[config]
disable_auto_feedback = true

[github_app]
pr_commands = []
handle_push_trigger = false
push_commands = []